# Audit: UI Refactor Plans — Remaining Gaps

Both iOS UI refactor plans (brave-pixel visual design + playful-knight code optimization) were **successfully applied** with 3 minor items remaining.

## Remaining Work

### 1. Add `centeredMinSpacer` to Spacing.swift
**File:** `apps/ios/betterwriter/betterwriter/Theme/Spacing.swift`
- Plan called for `static let centeredMinSpacer: CGFloat = 120`
- Was never added (other spacing constants like `textEditorMinHeight` and `focusDelayNs` were added)

### 2. Use spring animation for `.done` transition in RootView
**File:** `apps/ios/betterwriter/betterwriter/Views/RootView.swift`
- The main phase transition correctly uses `.spring(response: 0.5, dampingFraction: 0.85)`
- But the `.done` case inline transition still uses `.easeInOut(duration: 0.4)` — plan called for spring everywhere

### 3. Cache ISO8601DateFormatter + fix bare `try?` saves
**Files:**
- `apps/ios/betterwriter/betterwriter/Services/SyncService.swift` — `ISO8601DateFormatter()` created fresh each call; also has bare `try? modelContext.save()` on ~line 61
- `apps/ios/betterwriter/betterwriter/Services/APIClient.swift` — `ISO8601DateFormatter()` created inline on ~line 53
- `apps/ios/betterwriter/betterwriter/Views/ReadView.swift` — bare `try? modelContext.save()` on ~lines 201, 209

**Fix:** Add `private static let iso8601 = ISO8601DateFormatter()` as shared instance. Replace `try?` with `do/catch` + `print` logging.

## Verification
1. `bundle exec fastlane build` from `apps/ios`
2. Run in Simulator — verify `.done` phase uses spring transition
3. Check console output for save error logging when testing
